# Roadmap

Future ideas only. Roadmap entries are **not** action items.

Do not implement anything from this list unless the user explicitly asks for that feature or moves it into an active plan.

When the user mentions a future idea that is out of scope for the current task, add it here rather than implementing it opportunistically.

## Ideas

- Add the Angle Bracket generator after the flat L bracket constraint model is proven.
- Add bracket presets once there is enough real usage to justify saved configurations.
- Add richer local preview controls such as rotate, pan, zoom, and measurement overlays.
- Inspect the supplied `input/Examples.3mf` reference model in detail before expanding bracket families.
