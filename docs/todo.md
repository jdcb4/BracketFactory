# TODO / follow-ups

- Reintroduce Zod validation for form values (submit-time or field-level) compatible with Zod 4 + RHF.
- Optional outer-corner fillet geometry for L-bracket (parameter exists; geometry is still a stub).
- Code-split Three.js / heavy chunks to reduce main bundle size.
- Add more Playwright cases (guardrail messages, second template).

**Status:** MVP checklist from the build prompt is implemented; enable GitHub Pages + run the deploy workflow once the remote exists.
